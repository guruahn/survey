import React, { Component, PropTypes } from 'react';
const propTypes = {
};
const defaultProps = {
};
class SurveyDetail extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return(
            <div>SurveyDetail</div>
        );
    }
}
SurveyDetail.propTypes = propTypes;
SurveyDetail.defaultProps = defaultProps;
export default SurveyDetail;
